# A13-lists
This is a data collection and text formatting application written in C# (.NET Core 3.1). It collects user address information, stores it in a list, and applies custom title case formatting (capitalizing the first letter of each word) before displaying the results.
